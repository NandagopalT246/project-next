# Project NEXT — reasoning brain
# Get a key at https://aistudio.google.com/apikey
# The app runs the FULL scripted demo even with no key (graceful fallback),
# so this is only needed to make the rationale / self-critique / rewrite live.
GEMINI_API_KEY=
